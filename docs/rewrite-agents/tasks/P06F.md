# Task packet: P06F Flutter interaction remediation

TASK: P06F / `quiz_master-rq9.45`; blocks P06 / `quiz_master-rq9.7`
ROLE / MODEL / EFFORT: escalated Flutter specialist; `gpt-5.6-sol`; high
HUB: task `quiz_master-rq9.45`; worker `exec-flutter-P06F`; lead `lead`; reviewer `review-P06`
WORKSPACE: `C:\ap\quiz_master`, shared checkout with frozen uncommitted P06 files
BASE: `d927bc6`; preserve `.beads/issues.jsonl`, `PROJECT_OVERVIEW_RU.md` and all non-P06 paths
CONTRACT: accepted P04/P33 hashes stated in `docs/rewrite-agents/tasks/P06.md`

GOAL: Complete the narrow P06 interaction/router acceptance left after two fix cycles, while preserving the now-closed public/reveal/error decoders and localization/theme work.

READ FIRST: this packet; `docs/rewrite-agents/tasks/P06.md`; review-P06 hub verdicts #144/#146 and the latest exec-flutter-P06 blocker; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-flutter/SKILL.md`; `.agents/skills/test-driven-development/SKILL.md`; current P06 app/report/evidence.

OWNED PATHS: existing uncommitted `next/apps/quiz_app/**`, `docs/rewrite-agents/reports/P06.md`, `docs/rewrite-agents/evidence/P06/**`.
DO NOT TOUCH: every other path, Git/index/branches/commits/remote, Beads, system SDK installation. Do not spawn agents.

REQUIRED BEHAVIOR:

1. Preserve and test the final contract-model fixes: stable IDs, positive revision/64-hex hash, 4–6 unique options, and exact reveal answer variants with no mixed/empty/private forms.
2. Provide a visible, localized normalized-text submit control. A widget test must enter text through the UI, activate submit and assert the emitted typed value; disabled/submitting state must suppress all answer callbacks.
3. Provide keyboard focus and activation for choice controls. A widget test must focus a control, activate it with Enter/Space and assert the stable option ID. Maintain useful semantics labels/selected/disabled state.
4. Construct one GoRouter instance per app lifetime, not per locale/theme rebuild. Test navigation to gallery/join, theme and locale changes, back behavior and preservation of route/history/invite token.
5. Keep widgets maintainable; split files only when it clarifies ownership. Do not add new dependencies or scope.

ACCEPTANCE: record RED then GREEN evidence; `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, full `flutter test`, `flutter build web` and private-marker scan all pass. Android remains the already documented environment concern; do not retry or install tools. Update report/evidence/hashes. Post READY, no commit.

ESCALATE: if any interaction cannot be driven through the real widget tree in this single remediation, stop with the smallest failing test and do not replace it with direct callback invocation.
REPORT: DONE | BLOCKED.
