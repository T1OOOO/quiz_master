# Task packet: P33 live-team QR contracts

TASK: P33 / `quiz_master-rq9.34`
ROLE / MODEL / EFFORT: `qm_content` with live-team supplement; `gpt-5.6-terra`; high
HUB: task `quiz_master-rq9.34`; worker `exec-content-P33`; lead `lead`; reviewer `review-P33`
WORKSPACE: `C:\ap\quiz_master`, shared checkout
BASE: `d4ed49a`; preserve modified `.beads/issues.jsonl` (SHA-256 `0a317657685e1e4560d2664d07d6d22d64256d522e385c885c35d2a479c248b7`) and untracked `PROJECT_OVERVIEW_RU.md` (SHA-256 `1ed842b2f8b10ebc7fd16a58962abcc22631905935791da8719529873532e5e8`)
CONTRACT: accepted base `quiz-contract/v1`, content hash `5cc3275eb90dc71d58a5dc5d42be0fece52b727ead2bdf6ad9e7727a30df5f4e`; define `live-team-contract/v1` as its separately versioned extension

GOAL: Freeze executable live-team session, admission, identity, round, answer, audience/event, reconnect and result-access contracts before backend or Flutter consumers start.

READ FIRST: this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-contracts-content/SKILL.md`; `.agents/skills/qm-live-quiz/SKILL.md`; `docs/rewrite-agents/LIVE_TEAM_QUIZ_EN.md`; `docs/rewrite-agents/decisions/P33-live-team-policy.md`; `next/contracts/quiz-contract/v1/README.md` and its schemas/fixtures/checker; `docs/rewrite-agents/HUB_PROTOCOL_EN.md`.

OWNED PATHS: new `next/contracts/live-team-contract/v1/**`; new `docs/rewrite-agents/reports/P33.md` only.
DO NOT TOUCH: base `next/contracts/quiz-contract/v1/**`, all legacy/app/server/Flutter code, manifests, Beads, Git/index/branches/commits/remote, P34+ implementation, pre-existing dirty files. Do not spawn agents.

REQUIRED BEHAVIOR:

1. Provide closed, fully typed schemas and positive fixtures for session/settings/state, QR/manual invitation, join exchange and captain/resume identity, pinned round snapshot, final team answer/idempotent receipt, host/presenter/team/reveal events, reconnect replay/snapshot, and host/team/public result access.
2. Encode every decision in `P33-live-team-policy.md`, including shared correctness only after close, one captain/final submission, 50-team capacity, token/code forms and expiry/revocation, rate-limit parameters, no late join/pause, host transfer, unique answer key, serialized answer-close race, audience separation, replay semantics and public-share retention.
3. Reuse base stable IDs, revision/digest conventions, typed answer shapes and `scoring/v1`; pin bundle hash, question revision, option order and server deadline. Do not duplicate or weaken the grading engine.
4. Make event payloads separate by audience. Prove presenter/team pre-reveal payloads cannot contain selected answers, correctness, grading, answer keys, host/join/resume/share credentials or another team's data. Host arrival may contain only the committed selection and receipt metadata; it must not contain grading/correctness before reveal.
5. Provide named negative fixtures for: QR-to-host escalation; team-name takeover; expired/revoked/rotated invite; ambiguous/short/manual-code form; token leakage; join after lock/start; 51st team; second captain or unauthorized transfer; second/different answer; same-key/different-payload; stale round/revision/bundle; late and answer-close race outcomes; client-clock authority; presenter/team/host pre-reveal secret leaks; cross-team result access; expired/revoked/public-link overexposure; duplicate/gap/stale reconnect; missing persisted sequence/outbox linkage.
6. Add a deterministic, dependency-free checker that executes every schema against positives, checks cross-file invariants and proves every named negative rejects for the intended reason. Keep a production-grade validator, database transaction tests, cryptographic generation, throttling, WebSocket delivery and load measurements explicitly assigned to P34/P35/P38.

ACCEPTANCE: from `C:\ap\quiz_master`, run the checker twice with `python -B`; both exits are 0 with identical stdout/content and summary hashes; all JSON parses; no `__pycache__`; direct adversarial audience/credential/race/reconnect probes reject. Report commands, durations, hashes, negative names and any deliberately deferred implementation proof in `docs/rewrite-agents/reports/P33.md`. If the checker matches an ignore rule, call it out for force-add.

DEPENDENCIES: P04/P04R and P29 accepted; policy decisions are frozen in the lead ADR above. No product clarification remains for v1.
ESCALATE: stop for any need to modify the accepted base contract or policy ADR, any ambiguous audience/authorization rule, or after two substantive failed fix cycles. Post exact probe and required decision to hub.
REPORT: DONE | DONE_WITH_CONCERNS | NEEDS_CONTEXT | BLOCKED. Post READY; no commit.
