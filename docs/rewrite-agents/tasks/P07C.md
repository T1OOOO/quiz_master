# Task packet: P07C remote CI portability remediation

TASK: P07C / `quiz_master-rq9.46`
ROLE / MODEL / EFFORT: `qm_release`; `gpt-6-astra`; high
HUB: task `quiz_master-rq9.46`; worker `exec-release-P07C`; lead `lead`; reviewer `review-P07C`
WORKSPACE: `C:\ap\quiz_master`, shared checkout
BASE: `80a6d2c`; preserve modified `.beads/issues.jsonl`, untracked `PROJECT_OVERVIEW_RU.md`, accepted P04/P06/P07/P08/P09 artifacts and PR #1
OBSERVED RUN: GitHub Actions `35523769763`: Android passed with APK SHA-256 `9024d7f12908563e90a4a0c587450b45ad86ed1942b0db728e3350e86994996b`; Go failed because P08 source raw-byte SHA changed under LF checkout; Flutter failed because `rg` is absent on `ubuntu-24.04`.

GOAL: Make the pinned Quiz v2 workflow cross-platform and obtain one fully green PR run without changing legacy quiz prose, accepted bundle semantics, Android code or production behavior.

READ FIRST: this packet; `.agents/skills/qm-release/SKILL.md`; `.agents/skills/qm-contracts-content/SKILL.md`; `.agents/skills/test-driven-development/SKILL.md`; `.agents/skills/systematic-debugging/SKILL.md`; `.agents/skills/verification-before-completion/SKILL.md`; P07/P08 reports/evidence; `.github/workflows/quiz-v2-ci.yml`; P08 content hashing/tests/artifacts; P06 remote Android evidence; `docs/rewrite-agents/HUB_PROTOCOL_EN.md`.

OWNED PATHS: `.github/workflows/quiz-v2-ci.yml`; minimal `next/server/internal/content/**` source-hash implementation/tests; `next/content/home-alone-1-part-1/manifest.json`; P08 report/evidence files whose source/manifest hashes must change; new `docs/rewrite-agents/reports/P07C.md`; new `docs/rewrite-agents/evidence/P07C/**`.
DO NOT TOUCH: legacy `quizzes/**`, P04 contracts, P08 draft/bundle JSON, P06 Flutter/app code, P09 server behavior/migrations, root manifests/locks, Beads, Git/index/commits/remote, protected dirty files. Do not spawn agents.

REQUIRED FIX:

1. Define source SHA-256 over canonical source bytes: strict UTF-8 input with CRLF and lone CR normalized to LF before hashing. JSON cannot contain literal unescaped newlines inside strings, so this changes only transport line endings. Do not trim whitespace, reserialize JSON or hide other byte changes. Add RED/GREEN tests proving LF and CRLF equivalents hash identically while any other byte/content change differs.
2. Regenerate only the checked-in import manifest and dependent P08 source/manifest hash evidence. The canonical draft bytes/hash, controlled bundle bytes/hash, question/option mappings and grading must remain byte-identical. Preserve the P01 raw Windows checkout hash as historical evidence and clearly distinguish it from the new canonical source hash.
3. Replace the workflow Web marker scan's unavailable `rg` dependency with a runner-native strict scanner (for example GNU `grep`) whose exit 0 means marker found/fail, exit 1 means clean, and any other exit fails. Keep the exact six markers, scan the full `build/web`, and add a local shell syntax/behavior probe including missing-path/scanner-error behavior.
4. Do not weaken Go tests, privacy scanning, checksums, action/image pins or Android job. Do not merely install an unpinned package. Node action deprecation warnings are outside this remediation unless an already verified immutable pin can be updated without expanding scope.
5. Run sequential low-memory local gates: focused content tests, P08 independent verifier with updated expected canonical source/manifest hashes, full Go tests/vet where changed, Flutter marker-script probes, workflow YAML/pin checks, P04 unchanged hash and diff-check. Do not run local Flutter/Android/Docker builds.

ACCEPTANCE:

- Local LF/CRLF and mutation tests prove the canonical source-hash rule; real P08 manifest validates and two clean imports remain deterministic.
- P08 draft and bundle file hashes plus bundle SHA stay exactly accepted; only source/manifest-related hashes change and are reconciled in report/evidence.
- Workflow scan works on Ubuntu without `rg`, fails on a marker and scanner/path error, passes only on no matches.
- After lead commits/pushes, PR #1 produces one run where Go, Flutter and Android all pass. Record run/job/artifact IDs and do not claim device/release signing.
- Scoped diff-check passes; no temp/download/build artifacts remain locally. Report exact commands/durations/exits and hashes.

MCP/TOOLS: local Go/Python/shell for lightweight tests; GitHub is lead-owned for commit/push/run observation. No local Docker/Flutter/Android build.
ESCALATE: canonical source rule would alter draft/bundle bytes or semantics, workflow needs unpinned installation, remote runner fails for a new cause after one bounded diagnosis, or two substantive fix cycles.
REPORT: DONE | DONE_WITH_CONCERNS | BLOCKED. Post READY for local review before lead commit; final DONE requires lead-relayed green remote run.
