# Task packet: P09 authoritative attempt scoring and history

TASK: P09 / `quiz_master-rq9.10`
ROLE / MODEL / EFFORT: `qm_backend`; `gpt-6-astra`; high
HUB: task `quiz_master-rq9.10`; worker `exec-backend-P09`; lead `lead`; reviewer `review-P09`
WORKSPACE: `C:\ap\quiz_master`, shared checkout
BASE: `094474f`; preserve modified `.beads/issues.jsonl`, untracked `PROJECT_OVERVIEW_RU.md`, and all accepted P04/P05/P08/P33/P06/P07 artifacts
CONTRACT: accepted `quiz-contract/v1` hash `5cc3275eb90dc71d58a5dc5d42be0fece52b727ead2bdf6ad9e7727a30df5f4e`; accepted P08 bundle SHA-256 `6c7754aa9b142d8657bac1bb65f0536d30364d262ef109315c4b9356ee512b95`

GOAL: Add the first authoritative solo-attempt backend slice: load the reviewed controlled bundle, expose only its public catalog, start an authenticated pinned attempt, accept idempotent typed answers until a server deadline, finish with server-only scoring, and return participant-scoped history. Do not implement accounts, rooms, rankings, reveal UI or live-team behavior.

READ FIRST: this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-go-backend/SKILL.md`; `.agents/skills/qm-contracts-content/SKILL.md`; `.agents/skills/test-driven-development/SKILL.md`; `.agents/skills/systematic-debugging/SKILL.md`; `.agents/skills/verification-before-completion/SKILL.md`; accepted P04 attempts/public/error schemas and README; P05/P08 reports and server/content code; checked-in P08 bundle; `docs/rewrite-agents/HUB_PROTOCOL_EN.md`.

OWNED PATHS: new/updated `next/server/internal/attempts/**`; new migration(s) under `next/server/internal/migrate/migrations/**`; minimal integration edits/tests in `next/server/internal/config/**`, `next/server/internal/httpapi/**`, `next/server/cmd/api/**`; new `docs/rewrite-agents/reports/P09.md`; new `docs/rewrite-agents/evidence/P09/**`.
READ-ONLY INPUTS: `next/contracts/**`, `next/content/**`, legacy code/data, Flutter, infra/CI, P05 identity/store except imports through their public behavior.
DO NOT TOUCH: accepted P04/P08 contract/content artifacts, P05 migration `0001_identity.sql`, P05 identity token/session implementation, root manifests/locks, Flutter, infra/CI, Beads, Git/index/branches/commits/remote, protected dirty files. Do not spawn agents.

LEAD DECISIONS:

- The server loads exactly one configured controlled bundle at startup from `QM_CONTENT_BUNDLE_PATH`; default only for local development is `next/content/home-alone-1-part-1/bundle.json`. Validate its raw JSON, P04 schema/cross-record rules and bundle hash using P08 content code before serving or persisting. Startup fails closed on any mismatch. No hot reload or active-pointer system in P09.
- P05 internal participant IDs use `p_<32hex>`, while P04 consumer IDs require hyphens. Keep the accepted internal identity unchanged. At the attempt boundary derive the reversible external ID `p-<32hex>` from the authenticated internal ID; never accept participant identity from request JSON/headers, and map it back only for authorization. Add exhaustive conversion/rejection tests.
- Use PostgreSQL as source of truth. New immutable bundle row(s), attempts, question snapshots, answer receipts and finish history are migrated in a new ordered migration. Store internal participant FK separately from the external contract participant ID. Persist private grading only in server-owned bundle JSON/columns; never in public DTOs/logs/errors/evidence.
- Attempt IDs and receipt IDs satisfy P04 lowercase-hyphen ID grammar. An attempt pins bundle version/hash, `scoring/v1`, question revisions, shuffled option order and position-to-option-ID map once. Use cryptographic randomness in production with injectable deterministic seams in tests.
- The server sets `started_at` and `deadline_at` from an injected UTC clock and validated `QM_ATTEMPT_DURATION` (positive, bounded to 5 minutes–24 hours; local default 30 minutes). Clients cannot supply/extend either. Answer acceptance uses the transaction/database time boundary consistently; at exactly deadline the write is rejected.
- The authenticated caller supplies question ID/revision, typed answer, idempotency key and P04 payload digest. Recompute the digest over exactly external attempt ID, external participant ID, question ID, question revision and typed answer using P04 canonical JSON. Same key+same digest returns the original receipt; same key+different digest is a typed conflict. A question is final after its first accepted answer; a different key cannot replace it.
- Score only from the pinned private grading: single exact option ID; multi exact unique set with order ignored and no partial credit; normalized text through P08/P04 normalization. Ignore/reject any client score/correctness fields. Finishing is idempotent, requires at least one accepted answer, may occur after deadline, and persists score/history atomically. No speed bonus.
- Public catalog/start/answer/history responses never contain `private_grading`, correct IDs, accepted variants, per-answer correctness or explanations. P09 does not expose reveal. History is scoped to the authenticated participant; cross-participant IDs return the same non-leaking not-found/forbidden policy.

REQUIRED BEHAVIOR:

1. Add migration(s) with constraints/indexes/FKs for immutable bundle identity, participant-owned attempts, immutable snapshots, answer payload/digest/idempotency/receipt, timestamps/deadline/status and finished score. Enforce unique bundle hash/version, attempt/question final answer, attempt/key idempotency and receipt ID. Detect migration checksum drift through existing runner.
2. Implement a small attempts service/repository over pgx with injected clock, ID source and shuffler. Transactions lock the attempt row for answer/finish races, reject wrong owner/status/revision/answer kind/option IDs/duplicates/deadline, and never trust client grading.
3. Extend config/startup minimally to validate/load the P08 bundle and duration, persist/verify the immutable bundle, construct the service and preserve graceful shutdown/readiness. Do not add frameworks or speculative interfaces.
4. Add authenticated JSON HTTP endpoints under `/v1`: public current catalog; start attempt; submit one answer; finish; list/get own history. Use bounded request bodies, reject unknown/trailing fields, derive owner from auth context, use stable non-leaking error codes/statuses, and keep P04 consumer shapes exact where applicable.
5. Write tests first. Unit tests cover ID boundary, config/duration, canonical payload digest, shuffling/snapshot stability, typed answer validation, single/multi/text scoring, error mapping, strict HTTP JSON and secrecy scans. PostgreSQL integration tests cover bundle persistence/hash conflict, start snapshot, correct/incorrect/partial answers, idempotent replay/conflict, second-key replacement, cross-user access, revision/option forgery, deadline boundary, concurrent answer race, concurrent finish, partial finish, restart persistence and rollback.
6. Add an end-to-end disposable PostgreSQL test using the real P08 bundle and real bearer sessions. Prove public endpoints never include all private marker names, a nonzero legacy answer scores only its mapped stable option ID, forged client score/correctness is rejected, finish/history persist across service reconstruction, and server score equals an independent expected calculation.

ACCEPTANCE:

- `go test ./...`, `go vet ./next/server/...`, focused attempt/http tests and the tagged integration suite pass sequentially with bounded package parallelism appropriate for the low-memory host.
- A unique disposable PostgreSQL 17 project/container reaches healthy, new migrations apply, all integration/e2e cases run, and exact resources/volume are removed. Do not substitute mocks for this gate.
- P04 checker retains the accepted hash; P08 verifier/artifacts retain their accepted hashes. No accepted contract/content/source file or root manifest changes.
- Independently query the test DB to prove raw tokens/private keys are absent from attempt/receipt/history rows and public response captures; evidence redacts connection secrets and does not paste the complete answer key.
- Report exact commands/cwd/durations/exits, migration checksum/schema objects, endpoint matrix, test counts, container/project identity, cleanup proof and scoped file hashes. `git diff --check` passes on owned paths; no temp/cache/container remains.

MCP/TOOLS: local Go, filesystem and isolated Docker/PostgreSQL only. No production/staging DB, remote service, deployment or device.
ESCALATE: accepted contract cannot be honored without change, P08 bundle validation cannot be reused without editing accepted content, P05 identity boundary cannot be safely adapted, database unavailable after one bounded diagnosis, migration ownership conflict, memory pressure prevents sequential gates, or two substantive failed fix cycles.
REPORT: DONE | DONE_WITH_CONCERNS | BLOCKED. Post READY only after every honest gate; no commit.
