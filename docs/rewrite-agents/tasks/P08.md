# Task packet: P08 quizctl and first real legacy pack import

TASK: P08 / `quiz_master-rq9.9`
ROLE / MODEL / EFFORT: `qm_content`; `gpt-5.6-terra`; high
HUB: task `quiz_master-rq9.9`; worker `exec-content-P08`; lead `lead`; reviewer `review-P08`
WORKSPACE: `C:\ap\quiz_master`, shared checkout
BASE: `a2902cb`; preserve modified `.beads/issues.jsonl`, untracked `PROJECT_OVERVIEW_RU.md`, accepted contract/server/app/infra artifacts and all legacy quiz sources
CONTRACT: accepted `quiz-contract/v1` content hash `5cc3275eb90dc71d58a5dc5d42be0fece52b727ead2bdf6ad9e7727a30df5f4e`; P05 integration commit `a3d6062`

GOAL: Implement a deterministic Go `quizctl import/validate/build/diff` pipeline, migrate one checked-in legacy quiz into canonical private content plus a controlled bundle, and prove correct-answer mapping with executable golden/negative fixtures. Do not import the full corpus or publish content to a service/database.

READ FIRST: this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-contracts-content/SKILL.md`; `.agents/skills/test-driven-development/SKILL.md`; `.agents/skills/verification-before-completion/SKILL.md`; accepted P04 README/schemas/fixtures/checker; `docs/rewrite-agents/reports/P01.md`; `docs/rewrite-agents/evidence/P01/inventory.json`; `docs/rewrite-agents/reports/P05.md`; root `go.mod`/`go.sum`; `docs/rewrite-agents/HUB_PROTOCOL_EN.md`.

OWNED PATHS: new `next/server/cmd/quizctl/**`; new `next/server/internal/content/**`; new `next/content/**`; new `docs/rewrite-agents/reports/P08.md`; new `docs/rewrite-agents/evidence/P08/**`.
SHARED FILE LEASE: root `go.mod` and `go.sum` only if required for one maintained JSON Schema Draft 2020-12 validator; pin the dependency, explain the choice, and make no unrelated manifest change.
READ-ONLY INPUTS: `quizzes/**`, `next/contracts/quiz-contract/v1/**`, all accepted server/app/infra code.
DO NOT TOUCH: legacy quiz/source files, accepted contract files or their summary, database migrations, Flutter, infra/CI, Beads, Git/index/branches/commits/remote, protected dirty files. Do not spawn agents.

LEAD DECISIONS:

- The first real source pack is exactly `quizzes/Cinema/HomeAlone/home_alone_1_part_1.json` (P01 SHA-256 `2521e37a1187064c977dcbd8f1001b38f94016e26689333b214a07e1a0950439`, 25 questions). Import it without editing or fact-checking its prose.
- Legacy quiz/question IDs are mapped deterministically into the accepted lowercase ASCII ID grammar. Preserve every source-to-canonical mapping and source SHA-256 in a machine-readable manifest. Normalization collisions are fatal; never let traversal order or later files win. Option IDs are deterministic per question and the integer `correct_answer` maps to the corresponding stable option ID only after bounds checking.
- Missing or `null` `correct_multi` means single choice and must not override `correct_answer`. An empty array is also single choice for compatibility fixtures. A non-empty `correct_multi` is accepted only if it is an array of unique in-range integer indexes with at least two entries; it maps to `multiple_choice` and an exact stable-ID set. Any conflict/malformed value is a typed validation error.
- Difficulty mapping is explicit: missing or `null` -> `unknown`; integer 1-3 -> `easy`; 4-7 -> `medium`; 8-10 -> `hard`; other types/values are rejected. Category/title/description remain recorded as source metadata in the import manifest because accepted v1 draft schema has no such fields; do not infer category from English directory names.
- Canonical hashes use UTF-8 JSON with lexicographically sorted object keys, compact separators, unescaped Unicode, and no insignificant whitespace. Revision hashes are computed from the content object with its own `revision` omitted; bundle SHA-256 is computed with only `bundle_sha256` omitted, matching P04. `build` receives explicit bundle version and RFC3339 publication time; no wall-clock value may make output nondeterministic.
- This task creates controlled private artifacts, not a public/client payload and not a production publication. Private grading must never be emitted by `diff` unless the caller explicitly compares private inputs locally; evidence must not paste complete answer keys.

REQUIRED BEHAVIOR:

1. Build a small tested content library and runnable `go run ./next/server/cmd/quizctl` with subcommands:
   - `import`: strict single-file legacy JSON decoding, mapping and deterministic canonical draft/manifest output;
   - `validate`: full JSON Schema Draft 2020-12 validation against accepted local schemas plus cross-record rules that schema alone cannot express;
   - `build`: validate a canonical draft, derive public quiz/private grading, revisions and controlled bundle hash, and write deterministic output using explicit version/time arguments;
   - `diff`: deterministic semantic summary of added/removed/changed quiz/question/revision IDs with a documented zero/nonzero exit contract suitable for CI. It must not expose grading values in normal output.
2. Reject unknown legacy fields and trailing JSON values. Validate required nonblank strings, type `choice`, 4-6 nonblank options, answer indexes, duplicate quiz/question/option IDs, mapping collisions, malformed JSON/non-object questions, difficulty and `correct_multi`. Errors must include a stable code plus source path/question identity without secrets.
3. Add executable test-first golden cases for zero and nonzero `correct_answer`, first/last answers, 4/5/6 options, missing/null difficulty, missing/null/empty/non-empty `correct_multi`, and deterministic Unicode/canonical hashing. Negative cases must cover missing/negative/out-of-range answers, malformed/non-integer multi indexes, duplicate indexes/IDs, normalization collisions, wrong option counts, unknown fields, malformed JSON and schema-invalid/cross-record-invalid draft/bundle inputs.
4. Add cross-language normalization vectors for P04's NFC -> Unicode casefold -> Unicode-whitespace collapse/trim pipeline. Go tests must execute them. If exact Unicode casefold/NFC needs a maintained dependency, pin it under the manifest lease; do not substitute lowercase-only behavior.
5. Check in the first pack under `next/content/` with its canonical draft, source-map/import manifest and controlled bundle. Run import/build twice into isolated temporary directories and prove byte-identical artifacts and hashes. Prove the real pack has 25 source questions, 25 canonical questions and 25 private grading entries, with no missing/duplicate mapping.
6. Keep file writes atomic (temporary sibling + rename), refuse overwrite unless an explicit flag is supplied, and reject output paths outside the requested destination semantics. No database, network, editor, MCP, deployment or full-corpus migration.

ACCEPTANCE:

- From `C:\ap\quiz_master`, `go test ./...`, `go vet ./next/server/...`, and focused `quizctl` tests exit 0 without modifying accepted contract files or the legacy source.
- `quizctl validate` succeeds for the checked-in canonical draft and controlled bundle using a real Draft 2020-12 validator; direct negative schema probes fail for their expected reasons.
- Two clean import/build runs for the selected source are byte-identical. Source SHA, mapping counts, revision hashes and bundle SHA are independently recomputed in tests/evidence; `quizctl diff` reports no changes for identical builds and meaningful non-secret changes for a controlled mutation.
- Golden fixtures prove the legacy nonzero-answer bug cannot default to option zero. Every invalid answer/multi/difficulty/ID/collision case fails closed with a stable error code.
- P04 contract checker still exits 0 with the same content hash; `git diff --check` is clean on owned paths; no cache/temp/output outside `next/content/**` and P08 evidence is left behind.
- Report exact commands/cwd/durations/exits, dependency/version choice, source and artifact SHA-256 values, fixture counts, diff behavior and scoped file hashes. Do not claim full-corpus reconciliation or publication.

MCP/TOOLS: local Go/filesystem only; network solely to resolve a pinned Go validation/Unicode dependency if needed. No production/staging database or service.
ESCALATE: accepted schema cannot be validated/built without modification, selected source violates the frozen mapping rules, dependency/manifest lease must expand, correct-answer ambiguity cannot be resolved from source, or two substantive failed fix cycles.
REPORT: DONE | DONE_WITH_CONCERNS | BLOCKED. Post READY only after every honest gate; no commit.
