# Task packet: P03 environment contrac

TASK: P03 / `quiz_master-rq9.4
ROLE / MODEL / EFFORT: `qm_release`; `gpt-5.6-terra`; medium
HUB: task `quiz_master-rq9.4`; worker label `exec-release-P03`; lead recipient `lead`; later reviewer label assigned separately
WORKSPACE: `C:\ap\quiz_master`, shared checkou
BASE: `ce310d7589bc0f97adcb25d04a15c117aea7c1d8`; preserve pre-existing `.beads/issues.jsonl` and `PROJECT_OVERVIEW_RU.md`; lead-owned kit/status/task files are out of scope
CONTRACT: propose only; P04/P07 will freeze implementation/toolchain contracts

GOAL: Define the smallest reproducible local and CI environment for the rewrite, with observed prerequisites clearly separated from proposals and unknown external targets.

READ FIRST: `AGENTS.md`; this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-release/SKILL.md`; and `docs/rewrite-agents/HUB_PROTOCOL_EN.md`. Primary source pointers are `Taskfile.yml`, `docker-compose.yml`, `go.mod`, and `flutter/pubspec.yaml`. Inspect `Makefile`, `docker-compose.postgres.yml`, `Dockerfile`, `.env.example`, `BACKEND_TESTING.md`, or `docs/rewrite-agents/MCP_SELECTION_EN.md` only when a primary pointer reaches that launch/tooling concern; do not preload them all.

OWNED PATHS: `docs/rewrite-agents/reports/P03.md` and new files only under `docs/rewrite-agents/evidence/P03/`.
SHARED FILE LEASE: none.
DO NOT TOUCH: root/build/CI manifests, application sources, secrets, global tools, `.beads/**`, `PROJECT_OVERVIEW_RU.md`, Git index/branches, production/staging infrastructure and other reports.

REQUIRED BEHAVIOR:

- Inventory installed Go, Flutter/Dart, Java/Android, Docker/Compose and relevant browser capabilities without reading secrets.
- Inspect current startup/build entry points and note contradictions, including evidence related to `quiz_master-qqx`; do not fix them.
- Propose compatible pinned versions, one Windows/local startup sequence, one CI sequence, ports and isolated test-resource conventions.
- Confirm hub CLI identity and distinguish callable/configured/proposed/not-implemented MCP capabilities. Treat PostgreSQL identity as unverified unless independently proven to be a Quiz test target.
- Record concrete blockers, including unavailable engines/devices, without claiming release readiness.

ACCEPTANCE: A prerequisites table, proposed startup contract, isolation rules and blocker list. For every executed check, record the exact command, cwd, duration, exit status, concise result, evidence path and SHA-256 log/artifact hash. No deployment, migration, optional MCP installation or global mutation.
EVIDENCE: `docs/rewrite-agents/reports/P03.md`; optional logs under `docs/rewrite-agents/evidence/P03/`.
DEPENDENCIES: P00 accepted.
MCP/TOOLS: local read-only inspection and hub CLI; database/browser tools only after target identity is established.
ESCALATE: any need to deploy, access secrets, alter shared manifests, install software or decide a contract outside the report.
CONTEXT BUDGET: 400-900 words plus a compact versions table.
REPORT: standard status shape; post `READY` to `lead`; do not commit or mark hub done.
