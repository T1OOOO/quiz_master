# Task packet: P11S local SQLite rewrite API

TASK: P11S / `quiz_master-rq9.12.1`
ROLE: `qm_backend`; sole worker `backend-p11s`; no children
BASE: `82b0a48`; shared checkout `C:\ap\quiz_master`

## Goal

Add the smallest explicit local-development SQLite mode that lets the accepted P10 Flutter journey use the real rewrite API without Docker or PostgreSQL. Preserve the existing PostgreSQL production path, HTTP contracts, server-authoritative scoring, ownership, idempotency, private grading boundary and restart durability.

Read this packet, `qm-work-packet`, `qm-go-backend`, `test-driven-development`, P08/P09/P10/P11 reports, frozen contracts and current server/config/migrations before editing. Take hub task `quiz_master-rq9.12.1` as `backend-p11s`. Do not touch Flutter, CI, Compose, legacy app storage, content/contracts, Beads, `PROJECT_OVERVIEW_RU.md` or P11 evidence/report.

## Ownership and constraints

- Own only `next/server/**` plus narrowly necessary Go module files and new focused tests. Avoid dependencies: `modernc.org/sqlite` already exists in the root module.
- One command/test at a time; low-memory sequential work. No agents, Docker, PostgreSQL, GitHub Actions, SDK/package installation or network services.
- Use strict TDD: add a focused failing test, run it and record the expected RED failure before production code; then implement the minimal GREEN behavior. Preserve the RED/GREEN commands and outcomes in `docs/rewrite-agents/reports/P11S.md`.
- SQLite must be explicit, e.g. a `sqlite:` database URL/path. Never silently fall back from a PostgreSQL URL or connection failure. Reject unsafe/nonlocal use if the mode is exposed outside loopback.
- Do not duplicate scoring/auth rules in an alternate HTTP API. Introduce only the persistence boundary required by this concrete second backend. Keep transaction ownership and same-key/different-payload conflict behavior.
- SQLite data and timestamps must survive process restart. Private grading stays server-side and absent from catalog/pre-finish/error output.

## Required behavior

1. Config accepts an explicit local SQLite database URL while retaining current PostgreSQL validation and defaults. Invalid schemes/paths fail clearly.
2. Startup selects PostgreSQL exactly as before or initializes SQLite schema transactionally. Health checks report the actual database state.
3. SQLite supports guest create/auth, bundle pinning, attempt start, stable snapshots, answer idempotency/conflict, authoritative finish, owner-only reveal/history/list and restart persistence through the existing routes.
4. Concurrency/locking is safe for the single local process; constraints enforce uniqueness. SQL differences (`$n`, `for update`, JSON/time encoding, migrations) are handled deliberately, not by string replacement guessing.
5. Focused SQLite end-to-end test drives guest→catalog→start→answer→finish→reveal/history, closes/reopens the database and rechecks history. It also checks one ownership or idempotency boundary and public privacy.

## Verification and handoff

Run focused tests first, then the complete relevant Go suite sequentially. Run formatting and `go vet` for changed packages. Do not claim P11/Android acceptance. Report exact changed files, RED/GREEN evidence, test counts/exits, remaining risks and local launch variables in `docs/rewrite-agents/reports/P11S.md`. Post READY/BLOCKED to hub; do not commit or push.
