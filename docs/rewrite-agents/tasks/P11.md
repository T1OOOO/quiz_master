# Task packet: P11 first Web/Android vertical acceptance gate

TASK: P11 / `quiz_master-rq9.12`
ROLE: `qm_qa`; sole worker `verify-qa-P11`; no children
BASE: `9930563`; shared checkout `C:\ap\quiz_master`

## Goal

Independently exercise the accepted P09/P10 real Home Alone pack through a disposable PostgreSQL/API stack and the actual Flutter Web/Android targets. Verify that one authoritative attempt persists, server score agrees with submitted stable option IDs, explanations/history survive reconstruction, and no private keys enter public artifacts.

Read this packet, `qm-work-packet`, `qm-qa`, P07/P09/P10 reports, current Compose/Dockerfile/config, server routes, Flutter runtime configuration and P08 pack. Take hub task `quiz_master-rq9.12` as `verify-qa-P11`. This is verification: do not patch production code. Own only new `docs/rewrite-agents/reports/P11.md`, `docs/rewrite-agents/evidence/P11/**`, and narrowly necessary test harnesses under that evidence directory. Do not edit CI, app/server/contracts/content, manifests/locks, Beads or Git/index.

## Resource and safety rules

- One process/gate at a time. No parallel Flutter builds/tests, no concurrent Web and Android build, and no multiple agents.
- No GitHub Actions, remote CI, package/tool installation, SDK mutation, emulator creation or production/staging access.
- Docker resources must use a unique `qm-p11-<random>` project, loopback random ports and its own volume. Record exact identity before use and remove only that project/volume afterward, even on failure.
- Preserve `.beads/issues.jsonl` and `PROJECT_OVERVIEW_RU.md`. Never expose bearer tokens in evidence; redact or store only SHA-256/token-length metadata.
- Stop after the first reproducible infrastructure/product failure in a phase, preserve the minimal evidence, clean up, and continue only independent phases. Do not retry an unchanged failure more than once.

## Sequential phases

1. Record checkout hash, dirty protected files, Docker/Flutter/Chrome/Android readiness and free-memory snapshot. Reuse fresh unchanged P09/P10 unit evidence; do not rerun broad Go/Flutter suites.
2. Render and start the pinned Compose stack with a unique project and `--wait`. Verify exact image refs, health, random ports, migration 0003 and content paths. If startup fails, capture the first API/PostgreSQL log error, tear down exactly and classify setup vs regression. Do not patch.
3. If the stack is healthy, drive the real HTTP API with a small evidence harness: guest bootstrap, catalog (25 questions, no private markers), start, submit at least one known nonzero legacy answer plus enough answers to finish, reject forged score/private fields, finish, reveal/history ownership, replay/idempotency and restart persistence. Compare server score independently from P08 manifest/bundle without writing secrets to output.
4. Web: use the accepted Flutter source with a loopback `QM_API_BASE_URL`. Run one fresh `flutter build web`, scan the full output for the six private markers with strict scanner semantics, hash/manifest the artifact, and exercise the visible app in Chrome through guest→catalog→attempt→result→history when same-origin/CORS and stack availability permit. Capture inspected screenshots at phone and desktop widths. A build or screenshot alone is not a functional pass.
5. Android: inspect existing toolchain/device only. Run a fresh debug APK build and, if an existing connected Android target exists, the same visible journey. If `flutter doctor` reports the known missing cmdline-tools/no device, do not install or use Actions; record Android as unavailable. Historical P06 remote APK proves only the old foundation commit and cannot accept current P10.
6. Confirm API score/history match the Web/Android result, restart durability, private-marker absence, exact resource cleanup and no temporary credentials/artifacts outside the evidence/build paths. Report `DONE`, `DONE_WITH_CONCERNS` or `BLOCKED`; never claim an unavailable platform.

## Acceptance/report

P11 is accepted only if the same real pack completes on visible Web and an Android target/current APK, authoritative result persists and agrees with the API, and all privacy/cleanup checks pass. Otherwise report the narrow blockers and recommended owner/remediation task. Include commands, durations, exits, environment identity, artifact hashes/screenshots, and cleanup proof. Post READY/BLOCKED to the hub; do not commit or push.
