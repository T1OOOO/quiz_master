# Task packet: P01 legacy content baseline

TASK: P01 / `quiz_master-rq9.2
ROLE / MODEL / EFFORT: `qm_content`; `gpt-5.6-terra`; high
HUB: task `quiz_master-rq9.2`; worker label `exec-content-P01`; lead recipient `lead`; later reviewer label assigned separately
WORKSPACE: `C:\ap\quiz_master`, shared checkou
BASE: `d0dfc17057c163087bdc3390b22f6adfff3c3519` (legacy application content is unchanged from `ce310d7589bc0f97adcb25d04a15c117aea7c1d8`); preserve pre-existing `.beads/issues.jsonl` and `PROJECT_OVERVIEW_RU.md`; lead-owned kit/status/task files are out of scope
CONTRACT: no v2 contract yet; evidence consumes legacy JSON and Go decoding only

GOAL: Establish a reproducible inventory of the checked-in legacy quiz corpus and prove or disprove the suspected `correct_answer` to `correct_answer_index` decoding mismatch without touching production data.

READ FIRST: `AGENTS.md`; this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-contracts-content/SKILL.md`; `internal/quiz/domain/types.go`; `internal/quiz/service/service.go`; `internal/storage/repository/quiz.go`; `internal/storageserver/bootstrap.go`; representative `quizzes/**/*.json`. Read `docs/rewrite-agents/HUB_PROTOCOL_EN.md` once for lifecycle.

OWNED PATHS: `docs/rewrite-agents/reports/P01.md` and new files only under `docs/rewrite-agents/evidence/P01/`. A deterministic inventory/reproduction helper may live under that evidence subtree.
SHARED FILE LEASE: none.
DO NOT TOUCH: legacy Go/JSON sources, `.beads/**`, `PROJECT_OVERVIEW_RU.md`, Git index/branches, production databases, any `next/**` implementation, and all other task/report paths.

REQUIRED BEHAVIOR:

- Account for every JSON file under `quizzes/`, with file/question/category counts and distributions for question type, option count, missing difficulty and duplicate IDs.
- Record malformed/unreadable inputs individually; never silently skip them.
- Select or create an isolated fixture with a known nonzero `correct_answer`, exercise the current decoding path or the smallest faithful local reproduction, and compare the decoded/persisted field to the source value.
- Point to the exact structs/functions responsible. Distinguish checked-in source behavior from unknown production state.
- List migration hazards and the minimum golden fixtures needed for P04/P08. Do not correct legacy content.

ACCEPTANCE: Run the deterministic inventory and decode reproduction from `C:\ap\quiz_master`; record exact commands, versions, duration, exit codes and SHA-256 hashes for evidence files. Totals must reconcile to all discovered quiz files.
EVIDENCE: `docs/rewrite-agents/reports/P01.md`; artifacts/logs under `docs/rewrite-agents/evidence/P01/`.
DEPENDENCIES: P00 must be accepted before take/dispatch.
MCP/TOOLS: local filesystem, Go/Python standard tooling and hub CLI only; no database target and no optional server installation.
ESCALATE: missing/ambiguous schema, need to edit legacy sources, conflicting ownership, two failed substantive reproductions, or evidence suggesting production access is required.
CONTEXT BUDGET: target 400-900 words in the report plus compact machine-readable aggregates; return pointers, not corpus dumps.
REPORT: `DONE`, `DONE_WITH_CONCERNS`, `NEEDS_CONTEXT`, or `BLOCKED`, using the standard report shape. Post `READY` to `lead`; do not mark hub done or commit.
