# Task packet: P07 reproducible Compose and CI foundation

TASK: P07 / `quiz_master-rq9.8`
ROLE / MODEL / EFFORT: `qm_release`; `gpt-5.6-terra`; medium
HUB: task `quiz_master-rq9.8`; worker `exec-release-P07`; lead `lead`; reviewer `review-P07`
WORKSPACE: `C:\ap\quiz_master`, shared checkout
BASE: `9f8c07b`; preserve `.beads/issues.jsonl`, `PROJECT_OVERVIEW_RU.md` and all accepted application/contract files
CONTRACT: P04/P33 accepted; P05 server commit `a3d6062`; reviewed P06 code commit `313dda8`, with parent P06 still blocked only on Android build smoke

GOAL: Provide a reproducible, isolated local Compose path and pinned CI foundation for Go, PostgreSQL and Flutter Web/Android, and attempt the P06 debug APK in an Android-ready container without installing host SDK components.

READ FIRST: this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-release/SKILL.md`; `.agents/skills/verification-before-completion/SKILL.md`; `docs/rewrite-agents/reports/P03.md`; `docs/rewrite-agents/reports/P05.md`; `docs/rewrite-agents/reports/P06.md`; P05/P06 manifests and entrypoints; existing root Docker/Compose/CI files; `docs/rewrite-agents/HUB_PROTOCOL_EN.md`.

OWNED PATHS: new `next/infra/**`; new `.github/workflows/quiz-v2-ci.yml` under an exclusive lead lease; new `docs/rewrite-agents/reports/P07.md` and `docs/rewrite-agents/evidence/P07/**`.
DO NOT TOUCH: existing legacy Docker/Compose/Task/start files, server/app source or lockfiles, contracts, other workflows, Beads, Git/index/branches/commits/remote, protected dirty files. Do not spawn agents.

REQUIRED BEHAVIOR:

1. Add a multi-stage P05 API Dockerfile using Go 1.25 and a non-root runtime. Pin every base image by registry digest; verify requested architecture and record resolved image IDs/digests. Do not run `go mod tidy` or mutate locks.
2. Add an isolated Compose file for API + PostgreSQL 17 pinned by digest: no fixed container names, no host bind for database data, project-scoped named volume/network, health checks, service dependency, localhost/random-or-configurable host port, explicit non-secret example environment and graceful teardown for the exact project.
3. Document one Windows local startup path, bounded readiness check, logs, stop/volume cleanup and collision-safe project naming. Do not invoke legacy `start.sh`, absent `web/`, shared `./data` or production targets.
4. Add `.github/workflows/quiz-v2-ci.yml` with least permissions, concurrency cancellation and pinned action commits/tool versions. Required jobs/gates: Go 1.25 test/vet plus PostgreSQL integration; Flutter 3.47.1 format/analyze/test; Web build/private-marker scan; debug APK build on JDK 17/Android-ready runner; upload Web/APK checksummed artifacts. Keep signing and secrets out.
5. Add local validation that rejects mutable image tags/action refs, renders Compose config, builds API image, starts the isolated stack, waits for readiness, probes liveness/readiness and tears down exact resources. Capture commands/results without credentials.
6. For P06 Android smoke, use an Android-ready Flutter 3.47.1 container pinned by registry digest (or another already-approved isolated toolchain) to run `flutter build apk --debug` against `next/apps/quiz_app`. Do not install host SDK tools. Record artifact SHA-256 if successful; if image/tool/network prevents it after one bounded diagnosis, preserve exact blocker and do not close P06.

ACCEPTANCE:

- Docker/Compose configuration validation and pin checks exit 0; API image builds; isolated PostgreSQL/API stack reaches healthy state; `/health/live` and `/health/ready` return success; exact project is torn down with its volume.
- Existing `go test ./...`, P05 integration suite and P06 format/analyze/tests/Web build remain green without manifest mutation.
- Workflow YAML parses and every action/image/tool version is immutable/pinned. If remote GitHub Actions cannot be observed, say so; local config checks are not a remote CI pass.
- A real debug APK build must succeed to clear P06. An image pull or Android toolchain failure is evidence of an open blocker, not success. No device interaction or release signing claim.
- Report exact commands/cwd/durations/exits, image/action pins, container/project identity, artifact hashes, cleanup proof and scoped file hashes. No live deployment.

MCP/TOOLS: local Docker/Compose, Flutter/Go CLI and internet only for primary registry/tool metadata. No cluster, production DB or cloud deployment.
ESCALATE: need to edit existing root deployment files/locks, unpinned upstream dependency, container escape/system install, Docker failure after one bounded diagnosis, or two substantive failed fix cycles.
REPORT: DONE | DONE_WITH_CONCERNS | BLOCKED. Post READY only after honest gates, no commit.
