# Task packet: P02 current test baseline

TASK: P02 / `quiz_master-rq9.3
ROLE / MODEL / EFFORT: `qm_qa`; `gpt-5.6-luna`; medium
HUB: task `quiz_master-rq9.3`; worker label `exec-qa-P02`; lead recipient `lead`; later reviewer label assigned separately
WORKSPACE: `C:\ap\quiz_master`, shared checkou
BASE: `ce310d7589bc0f97adcb25d04a15c117aea7c1d8`; preserve pre-existing `.beads/issues.jsonl` and `PROJECT_OVERVIEW_RU.md`; current rewrite docs are lead-owned
CONTRACT: baseline-only task; no replacement contract consumed

GOAL: Establish the actual Go and Flutter test baseline at the recorded revision and diagnose the previously interrupted Flutter run without changing production code.

READ FIRST: `AGENTS.md`; this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-qa/SKILL.md`; `go.mod`; `BACKEND_TESTING.md`; `flutter/pubspec.yaml`; `flutter/test/widget_test.dart`; discovered `*_test.go` and `*_test.dart` entry points. Read `docs/rewrite-agents/HUB_PROTOCOL_EN.md` once.

OWNED PATHS: `docs/rewrite-agents/reports/P02.md` and new files only under `docs/rewrite-agents/evidence/P02/`.
SHARED FILE LEASE: exclusive ownership of Flutter test processes while this task is active; no codegen or build lease.
DO NOT TOUCH: application/test sources, dependency manifests/locks, `.beads/**`, `PROJECT_OVERVIEW_RU.md`, Git index/branches, and other reports.

REQUIRED BEHAVIOR:

- Record Go, Flutter and Dart versions.
- Run `go test ./...` with a finite timeout and captured exit status.
- Start Flutter diagnosis with one small known test and capture setup/runtime output. Run a broader suite only if the focused run establishes a bounded procedure.
- Separate pass, fail, timeout/interruption, skipped and unavailable results. A silent timeout is not a pass.
- Identify the smallest reproducible cause or boundary if Flutter remains stuck; do not upgrade dependencies or fix code.

ACCEPTANCE: Commands run from their stated working directories with bounded time, duration, exit code and log hashes. Report baseline failures separately from future regressions and reference related maintenance issue `quiz_master-4bs` without closing it.
EVIDENCE: `docs/rewrite-agents/reports/P02.md`; logs under `docs/rewrite-agents/evidence/P02/`.
DEPENDENCIES: P00 accepted.
MCP/TOOLS: local Go/Flutter tools and hub CLI; no browser/device requirement for this baseline packet.
ESCALATE: a diagnostic requires source edits, dependency upgrades, global installs, unbounded process ownership, or two failed substantive attempts.
CONTEXT BUDGET: concise result table and only the relevant tail/first failure from logs.
REPORT: standard status shape; post `READY` to `lead`; do not commit or mark hub done.
