# Task packet: P04 base contracts and scoring fixtures

TASK: P04 / `quiz_master-rq9.5`
ROLE / MODEL / EFFORT: `qm_content`; `gpt-5.6-terra`; high
HUB: task `quiz_master-rq9.5`; worker label `exec-content-P04`; lead recipient `lead`; reviewer assigned separately
WORKSPACE: `C:\ap\quiz_master`, shared checkout
BASE: `5e549a799bc8a14bc946240cd3a5005e305380ff`; preserve `.beads/issues.jsonl` and `PROJECT_OVERVIEW_RU.md`; legacy application and accepted reports are read-only
CONTRACT: create proposed `quiz-contract/v1`; P33 adds the live-team extension before backend/Flutter consumers start

GOAL: Define executable base content/card/attempt/room contracts and deterministic scoring fixtures that preserve answer secrecy and immutable revisions, while turning P01 migration hazards into explicit validation failures.

READ FIRST: `AGENTS.md`; this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-contracts-content/SKILL.md`; `docs/rewrite-agents/HUB_PROTOCOL_EN.md` once; `docs/rewrite-agents/reports/P01.md`; `docs/rewrite-agents/reports/P29.md`; `internal/quiz/domain/types.go`; `internal/quiz/service/service.go`; `internal/storage/repository/quiz.go`. Read TEAM_PLAN_EN.md sections 1, 6, 8-9 only. Do not load the whole corpus; use P01 aggregates and representative fixtures.

OWNED PATHS: new files under `next/contracts/**` and `docs/rewrite-agents/reports/P04.md`.
SHARED FILE LEASE: exclusive ownership of `next/contracts/**` for this task. No root/Go/Flutter manifests, codegen or SQL lease.
DO NOT TOUCH: legacy sources/content, backend/Flutter implementation, migrations, creative drafts, `.beads/**`, `PROJECT_OVERVIEW_RU.md`, STATUS/tasks/ADRs, Git/index/branches/commits/remote.

REQUIRED BEHAVIOR:

- Define canonical draft/published quiz/card JSON Schemas with stable quiz/question/option IDs, immutable revisions, bundle version/hash, locale/source/media metadata, 4-6 option bounds, unknown difficulty, and explicit single/multiple/normalized-text answer kinds.
- Separate private grading keys from public playable question DTOs. Define a reveal payload that adds only the permitted correct answer/explanation after reveal. No public fixture, error, log example or room event may contain a pre-reveal key.
- Define attempt start/snapshot/answer/finish/history shapes. An attempt pins question revision, deterministic option order/mapping and scoring-policy version. Accepted writes carry participant identity and idempotency key; same key/same payload returns the same receipt, same key/different payload conflicts.
- Define a base room/event envelope with session/round IDs, monotonic sequence/version, server timestamps and explicit audience. Keep team-specific join/scoring/reveal fields out of base v1 until P33; make the extension point explicit.
- Define an error envelope that cannot leak grading data. Include deadline/late, stale revision/round, authorization, validation and idempotency-conflict examples.
- Freeze proposed scoring fixtures for lead review: one point for exact single-choice ID; multiple choice requires exactly the expected ID set with no partial credit; text uses explicit accepted variants and a precisely documented Unicode/case/whitespace normalization pipeline; no speed bonus. Scoring is server-authoritative.
- Add positive and negative fixtures for 4/5/6 options, nonzero legacy answer mapping, zero answer, missing/negative/out-of-range answer, duplicate quiz/question IDs, absent/null `correct_multi`, malformed revisions, stale references and secret-field leakage. Unknown difficulty must not silently become zero.
- Provide a deterministic standard-library contract check that parses schemas/fixtures, validates required cross-file invariants and demonstrates negative fixtures are rejected. Do not add dependencies or edit manifests. State what requires stronger P08 quizctl/schema validation.
- Report legacy compatibility decisions still requiring the lead ADR. Do not claim P33 live-team rules or the pending reveal choice are settled.

ACCEPTANCE: From `C:\ap\quiz_master`, one documented command runs the contract checker twice with identical output/hash, accepts all positive fixtures and rejects every named negative fixture. JSON files parse, public/reveal/private separation assertions pass, all references resolve, and report records command/cwd/duration/exit/hash. No application code or manifest changes.
EVIDENCE: `docs/rewrite-agents/reports/P04.md`; checker, schemas, fixtures and generated deterministic summary under `next/contracts/**`.
DEPENDENCIES: accepted P01. P29 is accepted context. P33 must extend and be accepted before P05/P06 consume the combined contract.
MCP/TOOLS: local Python/Go standard tooling and hub CLI; no DB, browser, external MCP or optional install.
ESCALATE: cross-language text normalization cannot be specified consistently; base/team boundaries conflict; a schema requires a new dependency; or a scoring/reveal choice exceeds the stated proposals.
CONTEXT BUDGET: 600-900 word report plus compact schemas/fixtures and deterministic summary; return artifact paths rather than large payloads.
REPORT: standard status; post READY to `lead`; do not commit or mark hub done.
