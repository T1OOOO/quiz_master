# Task packet: P33F executable negative-fixture remediation

TASK: P33F / `quiz_master-rq9.44`; blocks execution child `quiz_master-rq9.43` and parent P33 gate `quiz_master-rq9.34`
ROLE / MODEL / EFFORT: escalated contract specialist; `gpt-5.6-sol`; high
HUB: task `quiz_master-rq9.44`; worker `exec-contract-P33F`; lead `lead`; reviewer `review-P33`
WORKSPACE: `C:\ap\quiz_master`, shared checkout with frozen uncommitted P33 files
BASE: `01c2a9c`; preserve modified `.beads/issues.jsonl`, untracked `PROJECT_OVERVIEW_RU.md`, and all non-P33 paths
CONTRACT: `live-team-contract/v1` over accepted `quiz-contract/v1` content hash `5cc3275eb90dc71d58a5dc5d42be0fece52b727ead2bdf6ad9e7727a30df5f4e`

GOAL: Replace the P33 negative-fixture dispatcher with honest executable evidence: every named case must supply data that is consumed by the relevant real schema, semantic or cross-record validator and must reject for an exact expected reason.

READ FIRST: this packet; `docs/rewrite-agents/tasks/P33.md`; review-P33 hub verdicts #106/#107/#109/#110 and worker blocker #111; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-contracts-content/SKILL.md`; `.agents/skills/qm-live-quiz/SKILL.md`; current `next/contracts/live-team-contract/v1/**`; `docs/rewrite-agents/reports/P33.md`.

OWNED PATHS: existing uncommitted `next/contracts/live-team-contract/v1/**` and `docs/rewrite-agents/reports/P33.md` only.
DO NOT TOUCH: the accepted base contract, policy ADR/task packets, legacy/app/server/Flutter code, manifests, Beads, Git/index/branches/commits/remote, protected dirty files. Do not spawn agents.

REQUIRED BEHAVIOR:

1. Give each negative fixture a closed operation-specific input shape. Prefer copy/mutate/validate records (`target`, `path`, mutation and validator) for schema/DTO cases; use explicit typed state/command inputs for admission, transfer, answer/close, replay and access cases. Include an exact `expected_error` identifier.
2. The dispatcher must read every declared input field, reject missing/unknown fields, execute the same schema/manual/cross-record validator used for positives, and accept a negative test only when the raised error identifier equals `expected_error`. A different incidental error is failure.
3. Remove tautological predicates, constant-false checks and case branches that ignore fixture values. Poisoning/replacing all non-operation input values with `IRRELEVANT` must not still yield the same set of 33 successful rejections; add a deterministic meta-check proving fixture inputs are material.
4. Preserve and verify the already-added recursive secrecy and policy-completeness work: nested presenter/host/reveal leaks reject; rotation is a required invite expiry trigger; operation/result, replay/snapshot and public result/retention payloads are closed and typed.
5. Keep the accepted base-hash pin and all P33 decisions unchanged. Do not expand into production implementation.

ACCEPTANCE: checker twice with identical stdout/content/summary hash; 33 cases reject for exact intended errors; input-poisoning probe fails closed instead of reporting the same success set; all positive schema/semantic instances pass; all JSON parses; no `__pycache__`. Update README/report with the remediated evidence and explicit meta-probe. Post READY; no commit.

ESCALATE: this is the architecture-change remediation. If it cannot satisfy the poisoning probe in one cycle, stop with the smallest failing case and do not add more hardcoded branches.
REPORT: DONE | BLOCKED, with command/cwd/exits/hashes and exact rejected names.
