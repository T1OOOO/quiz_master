# Task packet: P06 Flutter foundation and card primitives

TASK: P06 / `quiz_master-rq9.7`
ROLE / MODEL / EFFORT: `qm_flutter`; `gpt-5.6-terra`; medium
HUB: task `quiz_master-rq9.7`; worker `exec-flutter-P06`; lead `lead`; reviewer `review-P06`
WORKSPACE: `C:\ap\quiz_master`, shared checkout
BASE: `a6b930c`; preserve modified `.beads/issues.jsonl`, untracked `PROJECT_OVERVIEW_RU.md`, accepted contracts and server foundation
CONTRACT: `quiz-contract/v1` hash `5cc3275eb90dc71d58a5dc5d42be0fece52b727ead2bdf6ad9e7727a30df5f4e`; `live-team-contract/v1` hash `f3fd03ac03ffe346bf298590090269dd5139d77a5c9af4c264c0e88697797a86`

GOAL: Create the replacement Flutter Web/Android application shell with routing, RU/EN localization, accessible light/dark theming, a single typed API boundary and reusable public quiz card primitives proven against sanitized P04 fixtures.

READ FIRST: this packet; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-flutter/SKILL.md`; `.agents/skills/qm-live-quiz/SKILL.md`; `.agents/skills/test-driven-development/SKILL.md`; `.agents/skills/verification-before-completion/SKILL.md`; accepted contract READMEs/schemas/public fixtures; `docs/rewrite-agents/reports/P03.md`; legacy `flutter/pubspec.yaml` only for compatibility evidence, not code copying; `docs/rewrite-agents/HUB_PROTOCOL_EN.md`.

OWNED PATHS: new `next/apps/quiz_app/**`; new `docs/rewrite-agents/reports/P06.md` and `docs/rewrite-agents/evidence/P06/**`.
SHARED FILE LEASE: exclusive ownership of the new app's `pubspec.yaml`, lockfile, localization generation and generated Dart files for this task.
DO NOT TOUCH: legacy `flutter/**`/`mobile/**`, server/contracts, root manifests, infra/CI, Beads, Git/index/branches/commits/remote, protected dirty files. Do not spawn agents.

REQUIRED BEHAVIOR:

1. Generate only Android and Web platform shells under `next/apps/quiz_app`; package versions must resolve under Flutter 3.47.1/Dart 3.13.1 and be locked. Use Riverpod, Freezed, Dio and Go Router. Do not add GetIt, a second state framework, speculative editor/offline dependencies or private answer data.
2. Add a Material 3 app shell with light/dark/system theme support, RU and EN ARB resources, locale selection/fallback, Web URL history and Android back-safe routes. Required routes: home/catalog placeholder, public card fixture gallery and `/join/:inviteToken` placeholder that clearly treats the token as admission-only and never host authority.
3. Add one API client boundary with validated base URI, timeout configuration and typed decoding of the P04 non-leaking error envelope/public question/reveal shapes. No private draft/published grading model may be compiled into player code. Do not implement nonexistent business endpoints.
4. Implement reusable `PackTile`, `QuestionCard` and `ExplanationPanel`. `QuestionCard` supports single, multiple and normalized-text input, stable option IDs, 4/5/6-option layouts, disabled/submitting states, keyboard/focus/semantics and text scaling without overflow. Reveal/explanation is a separate explicit state, never inferred from public question data.
5. Derive test-only sanitized public/reveal cases from P04's accepted fixture. Never copy `draft`, `published_bundle`, `private_grading`, grading keys or accepted variant lists into runtime assets/generated Web output.
6. Write tests first. Cover model/error parsing and rejection of secret/unknown fields, route/deep-link behavior, RU/EN labels, theme selection, all three answer modes, 4/5/6 options, selection callbacks by stable ID, reveal separation, semantics/keyboard focus, narrow phone and wide projector layouts and large text scaling.

ACCEPTANCE:

- From `next/apps/quiz_app`: `flutter pub get`, required code/localization generation, `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, and `flutter test` exit 0.
- `flutter build web` exits 0; inspect generated output to ensure fixture/private secret markers (`private_grading`, `accepted_variants`, `correct_option_id`) are absent.
- Attempt `flutter build apk --debug`. Current `flutter doctor` reports missing Android cmdline-tools and no Android device. If build cannot run solely for that environment reason, record exact output and return DONE_WITH_CONCERNS/BLOCKED; do not call Android smoke passed and do not install system components outside the task. A build success is artifact smoke, not device interaction.
- Record screenshots or rendered test evidence at narrow phone and wide/projector sizes if available without broad UI automation; screenshots do not replace widget tests. Report commands/cwd/durations/exits, Flutter/Dart versions, package locks, test count and scoped hashes. No unowned edits.

MCP/TOOLS: Flutter CLI and local Chrome only; no live backend required. No production deploy/store signing.
ESCALATE: accepted contract mismatch, need for a private answer field in player code, root/legacy manifest change, unavailable Web build, or two substantive failed fix cycles.
REPORT: DONE | DONE_WITH_CONCERNS | BLOCKED. Post READY only after honest gate results, no commit.
