# Task packet: P10 Flutter catalog-to-history vertical flow

TASK: P10 / `quiz_master-rq9.11`
ROLE: `qm_flutter`; sole worker `exec-flutter-P10`; no children
BASE: `38867a2`; shared checkout `C:\ap\quiz_master`

## Goal

Replace the P06 fixture gallery as the primary app flow with a real API-backed vertical slice: guest bootstrap → catalog → authoritative attempt → answer all questions → finish → explanations/result → persisted server history. Preserve the P06 card primitives, localization, theme, join deep link and privacy boundaries.

Read this packet, `qm-work-packet`, `qm-flutter`, TDD, verification, P06/P08/P09/P10A/P10AF reports, current Flutter source/tests, server HTTP routes and public schemas. Take hub task `quiz_master-rq9.11` as `exec-flutter-P10` and post STARTED/READY. Do not spawn agents.

## Ownership and constraints

Own only `next/apps/quiz_app/**` plus new P10 report/evidence. You exclusively own Flutter dependency changes/codegen for this task. Do not edit server, contracts, content, migrations, CI, Beads, Git/index, or protected user files. Do not run GitHub Actions, Docker, Web/Android builds or emulators. Use sequential low-memory `flutter test`/`flutter analyze`; no parallel test shards.

Keep Dio, Riverpod, Go Router and existing Freezed state. Add only a small standard package if required for canonical SHA-256; no second state framework, service locator, persistence layer or speculative offline queue. Bearer credentials are created at runtime and held in memory only; never use query parameters, logs, fixtures, source constants, browser storage or compiled `dart-define` secrets. API base URL may use a non-secret compile-time setting with a validated loopback development default.

## Required behavior

1. Add strict closed decoders/models for guest response, catalog/public quiz, attempt/snapshots, receipts, finish/history, reveal list and error envelope. Reject unknown/private fields, invalid IDs/revisions/hashes/timestamps, malformed option orders and cross-record mismatches. Never decode or render grading maps or accepted variants.
2. Implement one Dio repository for `POST /v1/guests`, `GET /v1/catalog`, `POST /v1/attempts`, answer submission, finish, reveals and history. Apply Bearer only after bootstrap, JSON content type, finite timeouts and typed non-leaking failures. Compute P04 payload digest from canonical UTF-8 JSON with sorted keys/compact separators over exactly attempt ID, participant ID, question ID/revision and typed answer; generate a fresh bounded idempotency key per question and never send score/totals/correctness.
3. Use Riverpod state/notifiers for the journey. Show explicit localized loading, empty and retryable/nonretryable error states. A trimmed display-name form creates the guest session. Catalog displays the real P08 quiz through reusable `PackTile`; starting pins and applies the server's snapshot option order.
4. Play questions one at a time with `QuestionCard`, supporting the real 4/5/6 option range and future single/multi/text models. A choice is staged locally and submitted with an explicit localized Continue/Submit action; prevent double submission, disable controls while sending, advance only after an accepted receipt, and preserve navigation state across locale/theme rebuilds. Server is authoritative.
5. After the last accepted answer, finish exactly once, fetch owner reveals and history, verify reveal/history question revision and receipt alignment locally, then show server score plus reusable `ExplanationPanel` content. Provide a localized history route that reloads server history and a new-quiz action. Do not claim per-answer correctness before finish.
6. Routes must support Web history/back and Android back semantics without losing an active attempt or exposing credentials. Preserve `/join/:inviteToken`. Layout must remain usable at narrow phone width with large text and wide desktop/projector sizes; semantics, focus and keyboard activation remain intact.

## TDD and acceptance

Write widget/repository tests first and observe RED for each behavior group. Prefer an in-process deterministic HTTP test server or a narrow injected transport over mock-call assertions. Cover: successful 25-question real-shaped journey; exact API request bodies/headers/digest/idempotency; loading/error/empty/retry; 4/5/6 options; no early reveal; duplicate-tap suppression; history/result/back; RU/EN and theme stability; narrow large-text and wide layouts; malformed/secret-bearing responses rejected; credential absent from rendered text/loggable failures.

Run `flutter pub get` only if dependencies changed, `flutter gen-l10n`, codegen only if invalidated, formatter check, focused tests, full `flutter test` once, and `flutter analyze` once. Record commands/exits/durations and RED/GREEN evidence under `docs/rewrite-agents/evidence/P10/`; report `DONE`, `DONE_WITH_CONCERNS` or `BLOCKED` in `reports/P10.md`. P11 owns real browser/Android builds and end-to-end device verification; do not claim them here. Do not commit or push.
