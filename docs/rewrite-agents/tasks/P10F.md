# Task packet: P10F Flutter vertical review remediation

TASK: P10F / `quiz_master-rq9.11.2`
ROLE: same sole `qm_flutter` worker, hub label `exec-flutter-P10F`
BASE: current shared P10 READY diff; independent verdict: REJECT

Read P10/P10F packets, reviewer findings, `qm-work-packet`, `qm-flutter`, TDD and verification. Fix every finding below before another READY. Own only `next/apps/quiz_app/**` and P10/P10F report/evidence. No agents, Git/index, server/contracts/content, Docker, builds, emulator or GitHub Actions.

1. Make staged answers typed by `AnswerKind`. Normalized text must serialize as `{text: value}`, single as `{option_id: id}`, multi as `{option_ids: [...]}`. Add RED/GREEN wire tests for all three.
2. Before exposing an attempt, cross-check its bundle version/hash and every snapshot against the loaded catalog: exact question set/order, revision equality and exact option-ID permutation/position map. Reject unknown/stale/missing/duplicate options cleanly; no `.single`/`!` crash path.
3. Persist accepted receipts in journey state. On finish, require history question/revision/receipt IDs to match exactly the accepted submissions. Add mismatch negatives.
4. Store successful finish immediately before fetching reveals. Reveal/history retry must never POST finish again. Model retryable post-finish reveal failure separately and prove finish request count remains one.
5. Implement localized loading, catalog-empty, error and retry states; do not reject a valid empty catalog at decode time. Remove hard-coded English history error. Add EN/RU tests and retry behavior.
6. Replace the fake 25-question card test with a deterministic in-process API/widget journey that actually bootstraps, loads 25 questions, starts, applies pinned option order, explicitly submits all 25 without duplicate requests, finishes once, renders aligned explanations/server score, opens/reloads history and verifies back/theme/locale state. Programmatic compact fixtures are fine.
7. Require strict UTC RFC3339 instants for credential expiry, receipt accepted time and finish time; reject date-only and non-UTC-offset values. Add decoder negatives.
8. Split the 1,653-line `main.dart`: move wire models/repository and journey state into focused library files; leave app/routes/widgets readable. Do not introduce speculative layers or a second framework.
9. Resolve formatter truthfully: run formatter normally to apply changes, then the read-only formatter check must exit 0 without modifying files. Update reports/evidence to remove stale 24-test and false formatter claims.

Run focused RED/GREEN tests, then once each: localization generation, read-only formatter check, full `flutter test`, and `flutter analyze`, sequentially. Return READY only if the whole P10 packet and every finding are satisfied; otherwise report a concrete blocker, not a time limit.
