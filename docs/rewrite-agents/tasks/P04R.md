# Task packet: P04R contract pinning remediation

TASK: P04R / `quiz_master-rq9.42`; parent gate P04 / `quiz_master-rq9.5`
ROLE / MODEL / EFFORT: `qm_content`; `gpt-5.6-terra`; high
HUB: task `quiz_master-rq9.42`; worker label `exec-content-P04R`; lead recipient `lead`; reviewer `review-P04`
WORKSPACE: `C:\ap\quiz_master`, shared checkout with uncommitted P04 owned files
BASE: `0fdd9d6`; preserve `.beads/issues.jsonl`, `PROJECT_OVERVIEW_RU.md` and every non-P04 path
CONTRACT: proposed `quiz-contract/v1`; this remediation blocks P04 acceptance

GOAL: Close the four remaining critical binding/schema gaps from independent review #81 without adding features or changing the P33 boundary.

READ FIRST: this packet; `docs/rewrite-agents/tasks/P04.md`; `.agents/skills/qm-work-packet/SKILL.md`; `.agents/skills/qm-contracts-content/SKILL.md`; `docs/rewrite-agents/HUB_PROTOCOL_EN.md`; review-P04 hub verdict #81; current `next/contracts/quiz-contract/v1/**` and `docs/rewrite-agents/reports/P04.md` only.

OWNED PATHS: existing uncommitted `next/contracts/quiz-contract/v1/**`, `docs/rewrite-agents/reports/P04.md`, and new `docs/rewrite-agents/reports/P04R.md`.
DO NOT TOUCH: every other file, Git/index/branches/commits/remote, Beads, legacy/app code, manifests and P33 fields. Do not spawn agents.

REQUIRED BEHAVIOR:

1. Define the canonical published-bundle hash input as canonical UTF-8 JSON excluding only `bundle_sha256`. Verify the controlled bundle hash and require every attempt’s `bundle_version` and `bundle_sha256` to equal that bundle. Add a mismatch negative.
2. Bind reveal content to the exact canonical private grading and public option text for the pinned question revision. For text, reveal one permitted display answer without exposing all accepted variants. Add wrong-option/text negatives.
3. Define `payload_digest` as SHA-256 of canonical UTF-8 JSON containing `attempt_id`, `participant_id`, `question_id`, `question_revision`, and typed `answer` (excluding idempotency key). Recompute and verify it; add tampered-digest and answer/revision mismatch negatives.
4. Make all consumer-facing schema objects recursively closed and fully typed, including published quiz/private grading, question options/grading, reveal answer, attempt snapshot/write/receipt/history, room data/event and error details. Use `additionalProperties: false` (or a narrowly typed map with closed values). Implement a dependency-free schema-subset validator sufficient to execute all seven schemas against positive fixtures and negative extra/shape cases; do not merely inspect schema filenames.

ACCEPTANCE: Original six adversarial probes and these four probes reject. All named negatives reject for the intended reason. All positive instances validate through their JSON Schema plus cross-file invariants. Checker runs twice with identical stdout/content hash/summary hash; JSON parses; `python -B` creates no cache. Update README/P04 report and add P04R evidence report with command/cwd/duration/exits/hashes. Remove any `__pycache__`. Post READY; no commit.

ESCALATE: after one unsuccessful remediation cycle, stop and return exact failing probe; do not iterate again. Keep cross-language normalization and P33 reveal-policy decisions explicitly pending.
