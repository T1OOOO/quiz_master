# P00 preflight repor

Date: 2026-09-20
Task: P00 / `quiz_master-rq9.1
Workspace: `C:\ap\quiz_master

## Repository and ownership

- `git rev-parse --show-toplevel` resolves to `C:/ap/quiz_master`.
- Base revision is `ce310d7589bc0f97adcb25d04a15c117aea7c1d8`; `origin` is `https://github.com/T1OOOO/quiz_master.git`.
- The starting branch was `master`, aligned with `origin/master`. No other worktree or active rewrite hub task was present.
- The rewrite branch is `codex/quiz-v2`.
- Pre-existing dirty work was preserved: modified `.beads/issues.jsonl` and untracked `PROJECT_OVERVIEW_RU.md`. Neither is authorized for inclusion in a rewrite commit.

## Installed coordination ki

- Installed 11 custom-agent profiles under `.codex/agents`, 14 `qm-*` skills under `.agents/skills`, the complete planning document set under `docs/rewrite-agents`, and the project hub configuration in `.codex/config.toml`.
- Python `tomllib` parsed all 12 project TOML files. All 14 skill files have complete frontmatter fences.
- The current task did not load the newly installed custom profiles or hub MCP dynamically. The verified CLI fallback is `C:/Users/Alexey_Matvienko/tools/agent-hub/agent_hub.py`.
- Hub inbox review recovered the prior independent planning findings; the packaged plan already contains the corrected P05/P06/P25/P26 dependencies and P33 admission/result-link requirements.

## Tool and environment snapsho

| Capability | Observed |
|---|---|
| Git | 2.55.0.windows.3 |
| Beads | 0.43.0 (dev); onboarding/prime completed |
| Go | 1.27.0 windows/amd64 |
| Flutter / Dart | Flutter 3.47.1; Dart 3.13.1 |
| Java / Android | Temurin 17.0.17; adb 37.0.0 |
| Docker | Client 29.8.0; Linux engine unavailable at P00 |
| Hub | Existing CLI status/read succeeded; no active rewrite task before P00 |

`bd doctor` reports database integrity and DB/JSONL synchronization as healthy, but also reports a repository fingerprint mismatch, missing pre-push hook, no branch upstream, and two pre-existing merge artifacts. P00 does not mutate or clean those items.

## Durable evidence pointers

| Observation | Reproduction command / stable pointer | Recorded result |
|---|---|---|
| Checkout identity | `git rev-parse --show-toplevel`; `git rev-parse HEAD`; `git remote -v` from `C:\ap\quiz_master` | Target root, base commit and GitHub origin stated above |
| Starting dirty scope | Initial `git status --short --branch` before branch/kit writes | `master...origin/master`, modified `.beads/issues.jsonl`, untracked `PROJECT_OVERVIEW_RU.md` |
| Existing overview identity | `Get-FileHash -Algorithm SHA256 PROJECT_OVERVIEW_RU.md` | `1ED842B2F8B10EBC7FD16A58962ABCC22631905935791DA8719529873532E5E8` |
| Worktree/branch isolation | `git worktree list --porcelain`; later `git status --short --branch` | One worktree; rewrite branch `codex/quiz-v2` |
| Kit completeness | Source/destination SHA-256 comparison for 11 profiles, 14 skills, nine Markdown docs and config; semantic JSON comparison for two validation files | All pairs matched; JSON values were equal despite newline normalization |
| TOML/skills | Python 3.14 `tomllib` over `.codex/**/*.toml`; frontmatter fence check over `.agents/skills/qm-*/SKILL.md` | `toml_ok 12`; `skills 14 bad []` |
| Beads graph | Read-only SQLite query of `external_ref` and `dependencies`; `bd dep cycles --no-daemon` | 40 mappings, 70 required rewrite edges, no missing edges, no cycles |
| Hub state | `python C:/Users/Alexey_Matvienko/tools/agent-hub/agent_hub.py status` and `read --agent lead` | Existing planning sync only; no implementation tasks before P00 |

The Beads JSONL changed further when the requested rewrite registry was created, so its current hash is not presented as the preflight hash. The initial scoped Git status above and Git diff preserve the ownership boundary; the file remains excluded from rewrite staging.

## Beads registry

- Epic: `quiz_master-rq9`.
- Forty children map P00 through P39 using `external_ref` values `rewrite/P00` through `rewrite/P39`.
- Seventy explicit planning dependencies were checked directly against the Beads SQLite dependency table; none were missing and `bd dep cycles` reported no cycles.
- Existing issues are related rather than duplicated where appropriate: `quiz_master-4bs` to P02 and `quiz_master-qqx` to P03. P25 also depends on existing restore-verification issue `quiz_master-15g`.

## Product parity matrix

| Capability | Legacy/reference evidence | Replacement acceptance gate |
|---|---|---|
| Catalog and content | `quizzes/**/*.json`, legacy sync path | P08 one real pack; P12 complete reconciliation |
| Guest/account identity | Existing auth handlers/services | P05 identity boundary; P13 lifecycle/merge |
| Solo play and scoring | Existing quiz/auth APIs trust paths require reproduction | P09 server-authoritative scoring; P11 real Web/Android slice |
| Question/explanation cards | Existing Flutter client | P06 fixtures; P10 responsive real API flow |
| Multiplayer rooms/chat | Existing realtime and persisted room state | P15-P17 authorization, replay, reconnect and restart |
| Live team QR mode | New required scope | P33-P39; included in P25/P26 release gates |
| History/rankings/review | Existing app inventory | P13, P18-P20 |
| Offline practice | Not accepted as ranked play | P19-P20 user-scoped unranked queues |
| Editing/moderation | Existing administration inventory | P21-P24 validation and audit parity |
| MCP/CLI content tooling | New replacement tooling | P08 quizctl and P23-P24 official-SDK MCP parity |
| Migration/release | Legacy SQLite/Postgres paths and current deploy files | P25 restore/rollback; P26 artifacts; authorized P27 cutover |

## Scope and decision state

- Primary clients are Flutter Web and Android; `mobile/` is inventory only, not a second rewrite target.
- Legacy application and reference repositories stay intact until accepted migration/cutover gates.
- The live-team provisional policy remains shared reveal after close, with immediate host arrival updates and immediate committed receipts. P33 cannot be accepted until the pending correctness-visibility choice is explicitly settled.
- No production database, deployment or publication was inspected or changed during P00.

## P00 evidence verdic

Preflight evidence and the first three task packets are ready for independent review. P01-P03 must remain undispatched until P00 is independently accepted and integrated with a real commit.
