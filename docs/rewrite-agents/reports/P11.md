# P11 — first Web/Android vertical acceptance gate

Status: **BLOCKED** (Web build/privacy/UI smoke passed; API and Android acceptance unavailable under the explicit user constraints).

Worker: `verify-qa-P11`; task `quiz_master-rq9.12`; checkout `C:\ap\quiz_master`; verification began at source revision `0cbba40adb37dfed981045d3f720d71024747c91`; user constraints were finalized in packet revision `7f4bae8` without changing product source.

## Scope and protected state

No production code, CI, manifests, locks, Beads or `PROJECT_OVERVIEW_RU.md` were edited. Pre-existing protected dirt was observed: `.beads/issues.jsonl` modified and `PROJECT_OVERVIEW_RU.md` untracked. Only this report and `docs/rewrite-agents/evidence/P11/**` are worker-owned.

## Phase 1 — readiness

- Docker 29.8.0 and Compose v5.5.1 are installed, Flutter 3.47.1/Dart 3.13.1 is ready, Chrome 152.0.7977.84 is ready.
- `flutter doctor -v`: Android toolchain unavailable because `cmdline-tools` is missing; no Android device/emulator is connected. No installation or emulator creation was attempted.
- Free memory snapshot: 9,583,288 KiB of 32,981,432 KiB.
- Existing P09/P10 evidence was reused; broad suites were not rerun.

## Phase 2/3 — API stack and authoritative journey

Docker Compose and PostgreSQL were **skipped by direct user instruction**, not classified as a product failure. A disposable Compose attempt had created only `qm-p11-*` resources; the exact project, volume and network were identity-checked and removed. No `qm-p11-*` resources remain.

The requested local alternative is not available: `next/server` constructs `pgxpool.Pool`, config requires `QM_DATABASE_URL`, and migrations/attempts/identity/http integration all target PostgreSQL. The repository's SQLite implementation is legacy `internal/authdb`, `internal/storage` and test-only paths, not a persistence backend for `next/server`. No SQLite adapter or production change was added. Consequently guest bootstrap, catalog, authoritative attempt, score/finish/history/replay/restart checks cannot be run in this phase.

## Phase 4 — Web

Command (sequential, `next/apps/quiz_app`):

```text
flutter build web --release --dart-define=QM_API_BASE_URL=http://127.0.0.1:1
exit 0; duration 56.573 s
```

Flutter reported a successful Web build. A strict recursive scan of all 38 regular files in `build/web` found no occurrences of `private_grading`, `accepted_variants`, `correct_option_id`, `correct_option_ids`, `published_bundle`, or `"draft"`; scanner exit 0. SHA-256 manifest: `docs/rewrite-agents/evidence/P11/SHA256SUMS.txt`.

The artifact was served from loopback with Python's standard HTTP server. Visible browser inspection at the default desktop viewport showed the Quiz catalog guest form. Entering `P11 guest` and pressing Continue visibly reached the localized network error state (`Unable to load quizzes`, `Retry`), which is expected without an API. This is not a functional catalog-to-history pass. A phone-width screenshot and the API-backed journey were not claimed because no API was permitted/available.

## Phase 5 — Android

Android was not built or run: `flutter doctor -v` already reported missing `cmdline-tools`, and `adb devices` reported no connected target. No SDK mutation, emulator creation, Actions or historical APK was used. P06's old remote APK is not evidence for current P10/P11.

## Evidence and cleanup

- `evidence/P11/compose-run*.txt`: rendered isolated Compose identity and user-directed skip/cleanup trace.
- `evidence/P11/web-build-3.txt`: fresh Web build output, exit 0 and duration.
- `evidence/P11/web-scan.txt`: strict six-marker scan, exit 0.
- `evidence/P11/SHA256SUMS.txt`: 38-file artifact hash manifest.
- The loopback HTTP server was stopped with Ctrl-C; no server process remained. No credentials or bearer tokens were written to evidence.

Remaining blockers: an authorized disposable PostgreSQL or an existing supported rewrite-API-compatible persistence target is required for API/score/history/restart acceptance; a machine with installed Android cmdline-tools and a connected target is required for Android acceptance. Therefore P11 is not accepted and no publication/release claim is made.
