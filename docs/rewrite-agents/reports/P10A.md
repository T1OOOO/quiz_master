# P10A — real-client API prerequisites

Status: **DONE_WITH_CONCERNS** — implementation and all available Go/hash gates pass; the already-approved PostgreSQL target is unavailable, so the live integration execution remains pending.

`POST /v1/guests` now accepts only a closed `{display_name}` object, normalizes and validates the name, creates the digest-only guest session, and returns a closed no-store response containing the external participant ID, kind, display name, opaque token and UTC expiry. Invalid, ambiguous, oversized or malformed inputs return the generic non-leaking envelope. Identity and attempt patterns are registered on one exact `ServeMux`, avoiding catch-all shadowing.

Server configuration now carries `QM_CONTENT_MANIFEST_PATH`. Loopback development may select the manifest beside its configured bundle; nonlocal listeners require both paths explicitly. Attempt-service startup strictly loads that manifest and rejects quiz mismatch, missing/duplicate/unknown question mappings or blank explanations before serving.

Authenticated owners can call `GET /v1/attempts/{attempt}/reveals`. Missing and foreign attempts share the existing 404 behavior; unfinished owner attempts return the generic validation response. Finished attempts return only accepted-answer questions in pinned attempt order. Each item carries the pinned quiz/question/revision, one contract-valid display answer and the unchanged manifest explanation. Single choice returns stable ID plus public text; multiple choice returns stable IDs plus joined public text; normalized text returns only the first display variant. Responses are no-store and never return the grading map or accepted-variant list.

The real P08 HTTP integration test now bootstraps both bearer sessions through HTTP, checks pre-finish and stranger denial, validates every post-finish item against `reveal.schema.json`, proves only the two answered questions are returned in history order with real unchanged explanations, scans all other captures for private markers and both credentials, and rechecks history, score, finish idempotency and reveals after service reconstruction.

## Verification

Evidence is in `docs/rewrite-agents/evidence/P10A/`. Focused unit tests, tagged integration compilation, full `go test -p=1 -count=1 ./...`, and `go vet -p=1 ./next/server/...` all exited 0. P04 remains `5cc3275eb90dc71d58a5dc5d42be0fece52b727ead2bdf6ad9e7727a30df5f4e`; P08 bundle and file bytes remain unchanged. Exact commands, durations, hashes and observed RED/GREEN transitions are recorded in `checks.md`, `source-hashes.txt` and `red-green.md`.

Remaining gate: run the existing tagged PostgreSQL integration target when an approved disposable database is available. No Docker target was created. The worker ran no Git/index/commit/push command; scoped `git diff --check` remains a lead-relayed gate. No contract, P08 artifact, Flutter, migration, root manifest, CI, Beads or historical evidence file was edited.
